import Pagination from "../../components/Pagination";
import styled from "styled-components";
import { tokens } from "../../styles/tokens";

export const PaginationWithMargin = styled(Pagination)`
  margin-top: ${tokens.spacing.medium};
`;
